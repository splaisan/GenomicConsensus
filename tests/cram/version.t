This actually failed once because of a missing import, so we might as
well test it.

  $ variantCaller --version
  1.1.0
  $ variantCaller --versions
    GenomicConsensus version: 1.1.0
    ConsensusCore version: 1.0.1
    h5py version: 2.5.0
    hdf5 version: 1.8.11
